# Proyecto: Boilerplate Web (Flexbox + JS)

Este proyecto es un boilerplate (plantilla de inicio) para un sitio web moderno. Ha sido construido siguiendo las mejores prácticas de maquetación, semántica y responsividad.

## 📋 Características Principales

Este proyecto implementa las siguientes características:

* **Estructura Semántica:** Utiliza etiquetas HTML5 (`<header>`, `<main>`, `<aside>`, `<footer>`, `<article>`) para una mejor accesibilidad y SEO.
* **Maquetación con Flexbox:** El layout principal (`.wrapper`) y la mayoría de los componentes están construidos con CSS Flexbox para un posicionamiento flexible y moderno.
* **Diseño Responsive:** El sitio se adapta a diferentes tamaños de pantalla.
* **Menú Móvil Funcional:** Incluye un menú "hamburguesa" funcional en dispositivos móviles, controlado con JavaScript (`script.js`).
* **Estilos Modernos:**
    * Utiliza un **Reset CSS** moderno al inicio.
    * Usa **Variables CSS** (`:root`) para una fácil personalización de colores, fuentes y espaciado.
* **Componentes Reutilizables:**
    * Menú de navegación con submenús desplegables (`dropdown`).
    * Tarjetas de artículos (`.blog-post`).
    * Sidebar (`.sidebar`).
    * Pie de página con iconos de redes sociales.
* **Assets Externos (CDN):**
    * **Fuentes:** Carga de `Montserrat` y `Roboto` desde Google Fonts.
    * **Iconos:** Carga de `Material Symbols` de Google.

## 🛠️ Tecnologías Utilizadas

* **HTML5**
* **CSS3** (Variables, Flexbox, Media Queries)
* **JavaScript (ES6+)** (para la interactividad del menú móvil)
